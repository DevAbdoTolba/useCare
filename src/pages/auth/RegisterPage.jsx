import { Container, Typography } from '@mui/material';

export default function RegisterPage() {
  // TODO: implement per wireframe (docs/useCare.excalidraw).
  // Role selector (patient default / doctor with specialty). Submit just navigates.
  return (
    <Container maxWidth="sm">
      <Typography variant="h4" gutterBottom marginTop={6}>Register</Typography>
      <Typography color="text.secondary">TODO: implement.</Typography>
    </Container>
  );
}
