import { Container, Typography } from '@mui/material';

export default function LoginPage() {
  // TODO: implement per wireframe (docs/useCare.excalidraw).
  // See GitHub issue for detailed steps. Submit button should navigate
  // by role (admin -> /admin, doctor -> /doctor, patient -> /patient).
  return (
    <Container maxWidth="sm">
      <Typography variant="h4" gutterBottom marginTop={6}>Login</Typography>
      <Typography color="text.secondary">TODO: implement.</Typography>
    </Container>
  );
}
