import { Container, Typography } from '@mui/material';

export default function SystemSettingsPage() {
  // TODO: implement per wireframe (docs/useCare.excalidraw).
  // Basic toggles: clinic hours, default appointment duration, etc.
  return (
    <Container maxWidth="lg">
      <Typography variant="h4" gutterBottom marginTop={4}>System Settings</Typography>
      <Typography color="text.secondary">TODO: implement.</Typography>
    </Container>
  );
}
