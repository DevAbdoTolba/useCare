import { Container, Typography } from '@mui/material';

export default function UsersListPage() {
  // TODO: implement per wireframe (docs/useCare.excalidraw).
  // Table: Name | Role | Status. '?' badge for pending. Row click opens ApproveUserDialog.
  return (
    <Container maxWidth="lg">
      <Typography variant="h4" gutterBottom marginTop={4}>Users</Typography>
      <Typography color="text.secondary">TODO: implement.</Typography>
    </Container>
  );
}
