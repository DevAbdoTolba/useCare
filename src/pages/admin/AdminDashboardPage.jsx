import { Container, Typography } from '@mui/material';

export default function AdminDashboardPage() {
  // TODO: implement per wireframe (docs/useCare.excalidraw).
  // Quick stats: # pending approvals, # appointments today, # active doctors.
  return (
    <Container maxWidth="lg">
      <Typography variant="h4" gutterBottom marginTop={4}>Admin Dashboard</Typography>
      <Typography color="text.secondary">TODO: implement.</Typography>
    </Container>
  );
}
