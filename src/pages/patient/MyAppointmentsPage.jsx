import { Container, Typography } from '@mui/material';

export default function MyAppointmentsPage() {
  // TODO: implement per wireframe (docs/useCare.excalidraw).
  // Table: Date | Time | Doctor | Status | Edit/Delete actions (ConfirmDialog).
  return (
    <Container maxWidth="lg">
      <Typography variant="h4" gutterBottom marginTop={4}>My appointments</Typography>
      <Typography color="text.secondary">TODO: implement.</Typography>
    </Container>
  );
}
